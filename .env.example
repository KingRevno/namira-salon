# .env.example — commit this file, never .env.local
# Copy to .env.local for local development and fill in real values

NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_GA_ID=

# Contact form (if using a service like Resend, Formspree, etc.)
CONTACT_FORM_API_KEY=
